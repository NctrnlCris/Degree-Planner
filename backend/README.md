# Setup Instructions

- Step 1: setup virtual environment. Inside this directory run:
```
    python -m venv ./venv
    source ./venv/bin/activate
    pip install -Ur requirements.txt

```
- Step 2: run Flask. Inside this directory run:
```
    flask --app app run 
```